Part 1:
  npm packages: npm i expo-constants
    expo install react-native-gesture-handler   //also can use npm?
    npm i formik
    npm i yup

  suggestions: change PickerItem name to AppPickerItem (PickerItem is already a part of deprecated library)

Part 2:
  npm packages: expo install expo-image-picker
    expo install expo-permissions

PowerShell Issue: Get-ExecutionPolicy
  Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
  Set-ExecutionPolicy Restricted -Scope CurrentUser
  Set-ExecutionPolicy Undefined -Scope CurrentUser
